# yaml-language-server: $schema=compiled-properties.json
SkillToggleItem:
  item:
    type: STONE
    amount: 1
  skills:
    sunny_saturation:
      trigger:
        type: DAMAGED
        causes:
          - CONTACT
        
